<div class="header">
    <h1>Take Action!</h1>
    <h3>Here you can find my petition</h3>
</div>
<main>
    <div class="main-container">
        <div class="form">
            <form method="POST">
                {{#if error}}
                <h4>{{errorMsg}}</h4>
                {{/if}}
                <div class="canvas-submit">
                    <input id="canvas-url" type="hidden" name="signature">
                    <canvas id="canvas" width="400" height="100"></canvas>
                    <button>SUBMIT</button>
                </div>
            </form>
        </div>
    </div>
    <div class="main-content">

    </div>
    <script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
    <script src="script.js"></script> {{!-- in the right place because of canvas --}}
</main>
