<h2>Please log in.</h2>
<form method="POST">
    {{#if error}}
    <h4>{{errorMsg}}</h4>
    {{/if}}
    <div>
        <input type="text" id="email" name="email"><br>
        <input type="text" id="password" name="password"><br>
    </div>
    <button>LOGIN</button>
    <p>If you are not a member of this incredibly reactive community please <a href="/register">register here</a>.</p>
</form>